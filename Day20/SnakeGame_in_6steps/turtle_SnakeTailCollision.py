from turtle_SnakeScoreBoard_WallCollision import *

# def collision_tail():
#     for segment in turtles_list[1:]:
#         if turtles_list[0].distance(segment) < 10:
#             moving_snake = False
#             score_board.goto(0, 0)
#             score_board.write("GAME OVER", align="center", font="Courier")
